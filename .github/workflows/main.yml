# This is a basic workflow to help you get started with Actions

name: Build

on:
  push:
    branches: [ main, experimental ]
  pull_request:
    branches: [ main, experimental ]
  workflow_dispatch:

jobs:
  buildLinux:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4.1.7

      - uses: krdlab/setup-haxe@master
        with:
          haxe-version: 4.3.7
      
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libvlc-dev libvlccore-dev
      
      - name: Setup Haxelib and hxcpp
        run: |
          haxelib setup ~/haxelib
          haxelib install hxcpp > /dev/null --quiet
          # 构建 hxcpp
          cd $(haxelib path hxcpp)/../
          cd tools/hxcpp
          haxe compile.hxml
          cd ../../project
          neko build.n
          cd ../../
      
      - name: Run setup script
        run: |
          chmod +x ./setup/unix.sh
          sh ./setup/unix.sh
      
      - name: Skip SScript setup mode
        run: echo 'oy9:showMacroty8:loopCosti25y10:includeAllfg' >> ~/settings.cocoa
      
      - name: Create Version Tag
        run: echo "${{github.run_id}}" > VERSION
      
      - name: Compile
        run: haxelib run lime build Project.xml linux --app-version="4.0.0-${{ github.run_id}}" -D officialBuild
      
      - name: Publish Artifact
        uses: actions/upload-artifact@v4.3.4
        with:
          name: linuxBuild
          path: 'export/release/linux/bin'

  buildWindows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4.1.7

      - uses: krdlab/setup-haxe@master
        with:
          haxe-version: 4.3.7
      
      - name: Install Haxelib and build hxcpp
        run: |
          haxelib setup C:/haxelib
          haxelib install hxcpp > /dev/null --quiet
          cd %HAXELIB_PATH%\hxcpp
          cd tools/hxcpp
          haxe compile.hxml
          cd ..\..\project
          neko build.n
        shell: cmd
      
      - name: Run Windows setup
        run: .\setup\windows.bat
        shell: cmd
      
      - name: Skip SScript setup mode
        run: echo 'oy9:showMacroty8:loopCosti25y10:includeAllfg' >> %USERPROFILE%/settings.cocoa
        shell: cmd
      
      - name: Create Version Tag
        run: echo "${{github.run_id}}" > VERSION
      
      - name: Compile
        run: haxelib run lime build windows --app-version="4.0.0-${{ github.run_id}}" -D officialBuild
      
      - name: Publish Artifact
        uses: actions/upload-artifact@v4.3.4
        with:
          name: windowsBuild
          path: export/release/windows/bin

  buildMac:
    runs-on: macos-15

    steps:
      - uses: actions/checkout@v4.1.7

      - uses: krdlab/setup-haxe@master
        with:
          haxe-version: 4.3.7
      
      - name: Setup Haxelib and build hxcpp
        run: |
          haxelib setup ~/haxelib
          haxelib install hxcpp > /dev/null --quiet
          # 构建 hxcpp
          cd $(haxelib path hxcpp)/../
          cd tools/hxcpp
          haxe compile.hxml
          cd ../../project
          neko build.n
          cd ../../
      
      - name: Run setup script
        run: |
          chmod +x ./setup/unix.sh
          sh ./setup/unix.sh
      
      - name: Skip SScript setup mode
        run: echo 'oy9:showMacroty8:loopCosti25y10:includeAllfg' >> ~/settings.cocoa
      
      - name: Create Version Tag
        run: echo "${{github.run_id}}" > VERSION
      
      - name: Compile
        run: haxelib run lime build mac --app-version="4.0.0-${{ github.run_id}}" -D officialBuild
      
      - name: Publish Artifact
        uses: actions/upload-artifact@v4.3.4
        with:
          name: macBuild
          path: export/release/macos/bin